"""평가 하네스 — §6 측정 설계.

**정답셋은 에이전트가 읽는 DB 와 물리적으로 분리한다.** 이 패키지는 정답을 다루므로
`src/loregrind/` 안에 두지 않는다. 에이전트 런타임이 임포트할 수 있는 위치에 정답이
있으면 누출 경로가 하나 열린 것이고, 명명 정확도가 통째로 무의미해진다.

이 패키지의 코드는 전부 **결정론적**이다. 지표 계산 안에서 LLM 을 호출하지 않는다
(불변식 4). 의미 동등 판정에 모델이 필요한 경우는 판정 결과를 정답셋에 미리 넣어 두고
계산은 문자열 대조로 한다 — 그래야 같은 입력에 같은 숫자가 나온다.
"""
